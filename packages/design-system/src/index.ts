// @najafi/design-system — package entry.
//
// Tokens are consumed via the CSS entry: `@import "@najafi/design-system/styles.css"`.
// UI primitives (Button, Card, Input, …) are added in 阶段3 and re-exported here.
export * from './primitives'
